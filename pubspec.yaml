name: Internet_phone_monitor
description: Ứng dụng cảnh báo sử dụng mạng
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  connectivity_plus: ^5.0.1
  flutter_local_notifications: ^14.3.0
  permission_handler: ^10.0.0
  http: ^1.1.0
  provider: ^5.1.1
  background_fetch: ^1.0.0   
  workmanager: ^0.5.0        

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true